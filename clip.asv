function [x_clip, idx_clip] = clip(x, xlim)
% input: Column vector x and limits of cutoff
% output: clipped vector and original indices that are kept

idx = 1:numel(x);




t = datenum(V.time);
idx = 
tdat = h.t(h.t > min(t) - 1/12);
tdat = tdat(tdat < max(t) + 1/12);

end